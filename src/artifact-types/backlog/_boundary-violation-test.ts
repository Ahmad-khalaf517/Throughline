// TEMPORARY - deliberate boundary violation to prove the lint rule fires
// before any real code exists (Project Setup section 10, step 3). Delete this
// file once `pnpm lint` has been confirmed to fail on it, then confirmed to
// pass once it's gone.
import {} from '../ui-requirements';
